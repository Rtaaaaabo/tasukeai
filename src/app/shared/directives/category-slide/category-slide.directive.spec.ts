import { CategorySlideDirective } from './category-slide.directive';

describe('CategorySlideDirective', () => {
  it('should create an instance', () => {
    const directive = new CategorySlideDirective();
    expect(directive).toBeTruthy();
  });
});
